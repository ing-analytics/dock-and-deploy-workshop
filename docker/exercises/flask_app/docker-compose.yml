version: '3.8'
services:
  db:
    image: postgres:13
    environment:
      POSTGRES_USER: ###       
      POSTGRES_PASSWORD: ### 
      POSTGRES_DB: ###          
    volumes:
      -  db_data:/var/lib/postgresql/data
    networks:
      - ###               

  web:
    build: ./web
    ports:
      - ###:###
    depends_on:
      - ##
    networks:
      - ###                 

volumes:
  db_data:                    

networks:
  ###